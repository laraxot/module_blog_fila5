# BMAD Story — Blog PHPStan Fix

## Understand
- **Modulo**: Blog
- **Errori iniziali**: 413
- **Tipologia**: mixed cast, factory mancanti, return type
- **Regola**: `mixed` -> tipi concreti; cast sicuri

## Plan
1. Analizzare errori per tipo
2. Categorizzare: cast, factory, return type
3. Sistematizzare fix
4. Verificare con phpstan + phpmd + phpinsights + pest
5. Git sync

## Implement
### Step 1: Analisi
```bash
./vendor/bin/phpstan analyse Modules/Blog --error-format=table 2>&1 | head -100
```

### Step 2: Cast Errors
```php
// Pattern errore
(int) $mixed
(string) $mixed

// Pattern corretto
is_numeric($mixed) ? (int) $mixed : 0
is_string($mixed) ? $mixed : ''
```

### Step 3: Factory Errors
- Aggiungere `HasFactory` trait ai modelli
- O creare factory mancanti

### Step 4: Return Type
```php
/**
 * @return array<int, \Filament\Forms\Components\Component>
 */
protected function getFormSchema(): array
```

## Verify
- [ ] `./vendor/bin/phpstan analyse Modules/Blog` -> < 10 errori
- [ ] `./tools/phpmd.sh Modules/Blog/app`
- [ ] `./vendor/bin/pest --filter="Blog"`

## Status
- [ ] Da iniziare
